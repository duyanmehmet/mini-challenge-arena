[variables]
NODE_ENV = "production"

[phases.setup]
nixPkgs = ["nodejs_20", "python3", "gcc", "gnumake"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["echo 'Using ts-node directly'"]

[start]
cmd = "npx ts-node src/app.ts"
