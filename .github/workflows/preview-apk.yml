name: Preview APK

on:
  workflow_dispatch:

jobs:
  build:
    name: Build Android APK
    runs-on: ubuntu-latest
    timeout-minutes: 60

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node 20
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Setup Expo & EAS
        uses: expo/expo-github-action@v8
        with:
          eas-version: latest
          token: ${{ secrets.EXPO_TOKEN }}

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Install NDK and CMake
        run: |
          $ANDROID_SDK_ROOT/cmdline-tools/latest/bin/sdkmanager \
            "ndk;27.1.12297006" \
            "cmake;3.22.1" \
            "platforms;android-36" \
            "build-tools;36.0.0"

      - name: Install dependencies
        working-directory: mobile
        run: npm install

      - name: Build Preview APK
        working-directory: mobile
        run: eas build --platform android --profile preview --local --non-interactive

      - name: Locate APK
        working-directory: mobile
        run: |
          echo "=== Searching for APK files ==="
          find . -name "*.apk" -not -path "*/node_modules/*" 2>/dev/null
          echo "=== EAS build output dir ==="
          ls -la *.apk 2>/dev/null || echo "No APK in mobile/ root"

      - name: Find and upload APK
        uses: actions/upload-artifact@v4
        with:
          name: zeka-meydani-preview-${{ github.run_number }}
          path: |
            mobile/*.apk
            mobile/android/app/build/outputs/apk/**/*.apk
          if-no-files-found: error
          retention-days: 14
